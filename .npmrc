# 仓库内不写死 registry：它取自用户级 ~/.npmrc（内网 Nexus），换环境时也只改那一处。

# 显式声明严格链接：node_modules 下只有直接依赖，源码里 import 未声明的包会立刻暴露，
# 而不是像在 npm 的扁平结构里靠传递依赖碰运气解析成功。
node-linker=isolated

# 迁移前 npm/yarn 对 peer 冲突是宽松的，保持一致以免 pnpm 直接判失败；
# 真正需要关注的 peer 问题由构建与运行期验证兜底。
strict-peer-dependencies=false
auto-install-peers=true
